#include <iostream>
#include<bits/stdc++.h>
using namespace std;

int main() {
	int t;
	cin>>t;
	while(t--)
    {
	    int N, M, K;
        cin >> N >> M >> K;
        int a[N], count =0;
        for(int i=0; i<N; i++)
        {
            cin >> a[i];
        }
        for(int i=0; i<N; i++)
        {
            if(a[i] == 1)
            count += 100;
            else
        }

    }
    return 0;
}